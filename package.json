{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "surf-mcp",
  "scripts": {
    "typecheck": "bun run --filter \"*\" typecheck"
  },
  "workspaces": [
    "extension",
    "mcp-server",
    "shared"
  ]
}
